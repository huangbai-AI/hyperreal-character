---
name: hyperreal-character
version: 1.0.2
description: 超写实人像生图 + 生视频提示词工作流：按十层图片模板与八段视频模板撰写超写实人像（hyper-realistic portrait）提示词，配合支持文生图 / 图生视频的命令行生成工具完成 text_to_image 生首帧、image_to_video 生情绪视频的两段式生成。触发词：超写实人像、写实质感、皮肤质感、情绪视频、分秒拆解、首帧一致、AI 僵硬感、hyperreal、photorealistic portrait。
requires: kling-cli skill（配套生成工具的 CLI skill，负责所有实际生成调用）
---

# 超写实人像：生图 + 生视频提示词工作流

本 skill 提炼自一套经过验证的"超写实人像素材"提示词方法论（哭泣特写、
古装贵妇等多组已验证案例）。核心思想：

1. **两段式生成**：先用**图片提示词**生成一张超写实人物首帧图，确认满意后，
   再用**视频提示词**以该图为首帧做图生视频。图管"这个人长什么样"，
   视频管"这个人怎么演"。
2. **皮肤真实感是图片的最高优先级**；**首帧一致性 + 分秒拆解是视频的最高优先级**。
3. **所有生成必须走配套的命令行生成工具**（`kling` 命令），流程细节遵循
   `kling-cli` skill；本 skill 只负责把用户需求翻译成高质量提示词并编排两段式流程。

## 何时使用

- 用户要生成"超写实 / 电影感"的人像图片或人像情绪表演视频。
- 用户抱怨生成结果"像 AI"、"塑料脸"、"磨皮假脸"、"表情僵硬"，需要提示词层面修复。
- 用户给了人设（如"古装贵妇""现代都市白领"）和情绪（如"伤心""嗔怪""发怒"），
  要产出成片素材。

## 工作流总览

```
第 0 步  收集需求（人设 / 情绪 / 画幅 / 是否有台词）
第 1 步  按「图片提示词十层模板」撰写图片提示词 → 给用户过目
第 2 步  kling text_to_image 提交 → query_tasks 轮询 → 展示首帧图
第 3 步  用户确认首帧（不满意则改提示词重生，需用户同意后重投）
第 4 步  按「视频提示词八段模板」撰写视频提示词 → 给用户过目
第 5 步  kling image_to_video --image <首帧 works[].url> 提交 → 轮询 → 交付视频
```

> ⚠️ 每次提交都消耗灵感值。提示词先给用户看、用户点头再提交；失败不得静默改词重投
> （纪律细节见 `kling-cli` skill 的「计费、提交与重试纪律」）。

## 第 0 步：收集需求

至少确认以下四项，缺失时先问用户，不要脑补：

| 必问项 | 示例 |
|--------|------|
| 人设 | 年龄段、性别、族裔、气质定位（"32 岁王熙凤式贵妇"比"漂亮女人"好一个数量级） |
| 情绪与强度 | 伤心 8 分 / 嗔怪 6.5 分 / 憋笑 6 分（1–10 打分，直接写进视频提示词） |
| 画幅与景别 | 竖屏 9:16 / 极端脸部特写 / 中近景半身 |
| 台词 | 有无台词；有则确定台词文本、音色定位、语气 |

## 第 1 步：图片提示词 —— 十层模板

按顺序逐层组装，每层一段。完整讲解与可复制骨架见
[references/image-prompt.md](references/image-prompt.md)，范例见
[references/examples.md](references/examples.md)。

| 层 | 内容 | 要点 |
|----|------|------|
| 1 | 风格定调 | 超写实 / 日系胶片 / RAW 摄影 / 4K 电影肖像；一句话定媒介与质感 |
| 2 | 构图机位 | 景别、画幅、机位角度（3/4 侧、平视）、允许裁切的部位 |
| 3 | 人物基础 | 年龄 + 族裔 + 气质定位（给出具体"人设锚"） |
| 4 | 发型头饰 | 结构化描述：造型 → 细节 → 饰品逐件写，颜色可带 hex 值 |
| 5 | 服装配饰 | 材质、颜色（hex）、结构层次、工艺细节（刺绣 / 纽扣 / 项圈） |
| 6 | 五官锚点 | 眉形、眼型（丹凤眼≠圆眼）、双眼皮宽窄、虹膜色、鼻梁鼻头、唇形、脸型逐项写 |
| 7 | 妆容 | 底妆厚薄、眼影色系、眼线、睫毛、唇色质地（哑光/光泽） |
| 8 | **皮肤真实感（最高优先级）** | 毛孔、汗毛/毳毛、纹理、微瑕疵、轻微不对称 + 反磨皮否定词 |
| 9 | 光线 | 光源方向与性质（侧逆光/棚拍光）、轮廓光、明暗层次要求 |
| 10 | 背景+质量词 | 虚化散景（可带 hex）+ 收尾质量词（4K、胶片颗粒、浅景深） |

**皮肤层的固定要素**（这是"写实质感"的来源，不可省略）：

- 正向：清晰可见的细腻毛孔、真实皮肤纹理、细小汗毛/面部绒毛、自然肤色变化、
  极淡雀斑或少量细微瑕疵、额头鼻梁自然镜面高光。
- 否定：非磨皮、非塑料感、非 AI 平滑、不要蜡像感、不要 CGI 皮肤。
- 不对称：两眼大小开合微差、嘴角极细微不对称、眉毛高低自然差别——
  "精致但不完美"才有写实说服力。

## 第 2–3 步：生成首帧图并确认

```bash
kling who_am_i                       # 新会话先拿可用模型清单
kling text_to_image --model <从 who_am_i 选> --aspectRatio 9:16 "<十层模板组装的提示词>"
kling query_tasks <generationId>     # 轮询至 COMPLETED，展示 works[].url
```

- 模型必须显式指定（取自 `who_am_i`），CLI 不会自动选。
- 首帧图必须给用户确认后才进入视频阶段——视频阶段的一切一致性都锚在这张图上。

## 第 4 步：视频提示词 —— 八段模板

完整讲解与可复制骨架见 [references/video-prompt.md](references/video-prompt.md)。

| 段 | 内容 | 要点 |
|----|------|------|
| 1 | 画面配置 | 画幅 + "写实质感情绪表情视频" + 机位（手持微晃/固定）+ 景别 |
| 2 | **首帧一致性锚定** | "以当前图片作为首帧""保持人物身份/脸型/发型/服装/妆容/背景不变"，并**重申五官锚点**（防转头漂移：如"丹凤眼而不是大圆眼"） |
| 3 | 皮肤质感重申 | 把图片提示词第 8 层压缩重申一遍 |
| 4 | 人设 + 声音设定 | 气质定位（含"不是什么"的排除法）；有台词时定音色、语速、语气 |
| 5 | 情绪设定 + 强度分 | 情绪的内在逻辑（"不是崩溃，而是眼里的光暗下去"）+ 1–10 强度分 |
| 6 | **分秒拆解** | 按 0.5–1.5 秒粒度逐段写微表情/眼神/呼吸/头颈动作，动作要有因果链 |
| 7 | 台词与口型 | 台词文本、口型清楚、声音质感（低/轻/沙/带笑）、尾音处理 |
| 8 | 表演要求 + 反向提示词 | 表演逻辑链总结 + 固定否定串：不要字幕、不要水印、不要夸张表演、不要 AI 僵硬感 |

**分秒拆解的写法要领**：

- 每段 0.5–1.5 秒，只写"可被镜头看见"的物理变化（眼皮、嘴角、鼻翼、肩膀、呼吸）。
- 动作必须有心理因果链：先察觉 → 先表情变化 → 再转头 → 身体跟上（不要凭空动作）。
- 转头/转身段落必须再次强调五官不变（防止转正脸后"变脸"）。

## 第 5 步：生成视频

```bash
kling image_to_video --model <从 who_am_i 选> --image "<首帧 works[].url>" \
  --duration <秒数> "<八段模板组装的提示词>"
kling query_tasks <generationId>     # 轮询至 COMPLETED，展示 works[].url
```

- `--image` 直接传首帧任务返回的 `works[].url`，无需下载重传。
- 文生视频约 2–8 分钟，按 `kling-cli` skill 的轮询节奏逐次查询并向用户实时反馈。
- 交付时明文附上资源链接并提醒用户及时保存（链接有保留期限）。

## 常见翻车与修复

| 症状 | 修复 |
|------|------|
| 塑料脸 / 磨皮假脸 | 图片提示词第 8 层缺失或太弱：补毛孔/汗毛/微瑕疵 + 反磨皮否定词 |
| 转头后"变脸" | 视频提示词第 2 段没重申五官锚点；在转头段落再写一遍"丹凤眼而不是大圆眼"式排除 |
| 表情假 / AI 僵硬 | 分秒拆解粒度太粗或缺因果链；拆到 0.5–1 秒粒度并补"先…随后…"逻辑 |
| 情绪过火变夸张表演 | 加情绪强度分（≤6 分）+ 排除法（"不是崩溃，不是大喊，而是…"） |
| 口型对不上 | 台词段单独写：口型清楚 + 台词发音拆解 + 尾音处理 + 时长预算 |
| 五官稚嫩化漂移 | 人设段用排除法钉死："不是稚气撒娇，不是软萌卖甜，是 32 岁成熟贵妇" |

## 合规声明

- 本 skill 生成的均为**虚构人物形象**，不得用于仿冒、拼接真实人物肖像，
  不得制作虚假信息或侵权内容。
- 发布 AI 生成内容时，请按所在平台规范对内容进行显著的 AI 生成标识。
- 请遵守所用生成服务的内容政策与当地法律法规。

## 与 kling-cli skill 的分工

- 本 skill：需求收集、提示词撰写、两段式流程编排、质量修复。
- `kling-cli` skill：安装登录、`who_am_i` 能力发现、提交/轮询/计费纪律、错误处理。
  **所有 `kling` 命令的调用规范以 `kling-cli` skill 为准。**
